export enum UserType {
  CANDIDATE = 'Candidate',
  COMPANY = 'Company',
}

export enum LocationType {
  HYBRID = 'Hybrid',
  REMOTE = 'Remote',
  ONSITE = 'Onsite',
}

export enum GenderTypeDto {
  MALE = 'Male',
  FEMALE = 'Female',
  OTHER = 'Other',
}
